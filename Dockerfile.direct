FROM python:3.9-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    redis \
    pyzmq \
    pandas==2.0.3 \
    numpy==1.24.3

COPY mt5_connector_direct.py .

CMD ["python", "mt5_connector_direct.py"]
